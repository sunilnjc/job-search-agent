// Copy to Local.xcconfig (gitignored), then set YOUR public project settings.
// xcconfig treats // as a comment; use the /$()/ separator shown below.
API_BASE_URL = https:/$()/www.thejobpursuit.com
SUPABASE_URL = https:/$()/your-project.supabase.co
SUPABASE_PUBLISHABLE_KEY = your-public-publishable-key
